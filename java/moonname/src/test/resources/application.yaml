spring:
  datasource:
    url: jdbc:h2:mem:moonname-context;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
